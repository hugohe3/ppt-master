# 样板说明

本目录是浸没式光刻缺陷工程周报样板。视觉语言与 `spec_lock.md` 锁定，页结构按本周会类型重排，不是填空母版。

全部数字、lot、工具编号为**演示用虚构数据**。机制名称来自公开文献词汇。

给公司 Agent：锁 `spec_lock.md` 的配色/字体/blueprint，按你的真实材料重写 `svg_output/`，禁止生图，质量门 0 blocking error 后再导出。
